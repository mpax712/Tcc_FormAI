@extends('layouts.app')
@section('title', 'Aceitar convite · FormAI')
@section('content')
<div class="row justify-content-center"><div class="col-md-7"><div class="card p-4"><h1 class="h3">Entrar em {{ $invitation->classroom->name }}</h1><p>Convite destinado a <strong>{{ $invitation->email }}</strong>.</p><form method="post" action="{{ route('invitations.store', $token) }}">@csrf<div class="mb-3"><label class="form-label" for="name">Seu nome</label><input class="form-control" id="name" name="name" value="{{ old('name', auth()->user()?->name) }}" required></div><div class="row"><div class="col-md-6 mb-3"><label class="form-label" for="password">Senha</label><input class="form-control" id="password" name="password" type="password" minlength="{{ config('formai.password_min_length') }}" autocomplete="new-password" required><div class="form-text">Mínimo de {{ config('formai.password_min_length') }} caracteres.</div></div><div class="col-md-6 mb-3"><label class="form-label" for="password_confirmation">Confirmar</label><input class="form-control" id="password_confirmation" name="password_confirmation" type="password" minlength="{{ config('formai.password_min_length') }}" autocomplete="new-password" required></div></div><button class="btn btn-primary" type="submit">Aceitar convite</button></form></div></div></div>
@endsection
